<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el CheckBox está marcado -->
    <item android:state_checked="true" android:color="@android:color/white" /> <!-- Color negro para la flechita cuando está marcado -->
    <!-- Estado cuando el CheckBox no está marcado -->
    <item android:color="@android:color/black" /> <!-- Color negro por defecto -->
</selector>
